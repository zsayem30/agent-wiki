# Optional Host Agent Examples

These are example prompts for host-project agents. They are not active defaults
inside `agent-wiki/`.

Copy or adapt them into the host project's root `opencode.json` if you want
project-specific agents for implementation, literature search, or reporting.

Default bundled active role:

```text
agent-wiki/.agents/wiki-curator.md
```
