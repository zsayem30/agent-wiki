# Runs

This directory is for compact human-readable run summaries if needed.

Structured run cards belong in:

```text
knowledge/experiment_registry.yaml
```

Generated artifacts belong in:

```text
results/
```

Keep this directory as an index or compact narrative, not a dumping ground for
raw experiment logs.

